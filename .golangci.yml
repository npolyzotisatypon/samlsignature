version: "2"
run:
  go: "1.25"
  tests: true
linters:
  enable:
    - gosec
    - misspell
    - revive
  settings:
    errcheck:
      check-blank: true
    revive:
      confidence: 0.8
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
